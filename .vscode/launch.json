{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Preview Biolume",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/samples"
      ],
      "preLaunchTask": "build"
    }
  ]
}
